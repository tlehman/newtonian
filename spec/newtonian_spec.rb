require 'spec_helper'

describe Newtonian do
  it 'has a version number' do
    expect(Newtonian::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
